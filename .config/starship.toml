add_newline = false


format = "$directory$git_branch$git_status$package$memory_usage$character"

[directory]
truncation_length = 1

[character]
success_symbol = "[->>](green)"
error_symbol = "[->>](red)"
vicmd_symbol = "[<<-](green)"

[git_branch]
format = "[$symbol$branch]($style) "
